import SwiftUI

struct DiscoverView: View {
    var body: some View {
        ZStack{
            Color.blue
            Text("Discover")
        }
        
    }
}

struct DiscoverView_Previews: PreviewProvider {
    static var previews: some View {
        DiscoverView()
    }
}
